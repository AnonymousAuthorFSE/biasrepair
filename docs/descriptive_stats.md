# Descriptive statistics

Run `python scripts/descriptive_stats.py --dataset_dir data/dataset` to compute counts per category (and per source if a `source` field exists in the labels). The script writes its output to this file.
